Place the provided logo image file here so the map page shows it in the top bar.

Steps:

- Save the logo image you attached in this conversation with the filename `geointellab_logo.jpeg`.
- Put the file at `assets/geointellab_logo.jpeg` (relative to the project root).
- If you prefer PNG or another name, update the `src` attribute in [control_grid_map.html](control_grid_map.html#L236).

If you want me to embed the image file into the repo directly, upload the image here or tell me and I will add it.
